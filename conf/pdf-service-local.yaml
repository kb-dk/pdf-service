#
# This config contains developer specific overrides, such as read-only account credentials and test server locations.
# If will be automatically merged with the behaviour and the environment config when accesses through the application
# config system.
#
# The environment config is controlled by the individual developer and is not part of the code repository.
#
config:
  basePath: /home/pech/projects/pdf-service
#  basePath: /home/pech/testpdfs
  resourcesDir: src/main/resources/
  outputDir: src/main/resources/output/

  limits:
    max: 10000
